using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GameObjectSpawner : MonoBehaviour
{
    //[SerializeField] private GameObject gameObjectPrefab;

    //[SerializeField] private float spawnInterval = 1;

    //[SerializeField] private bool isSpawning = true;

    //public delegate void OnSpawnGameObject(GameObject go); // wat gegeven wordt als een listenere wordt een functie 

    //private event OnSpawnGameObject onSwpawnGameObject;




    // Start is called before the first frame update
    //void Start()
    //{
        //StartCoroutine(SpawnGameObject());
    //}

    //IEnumerator SpawnGameObject() 
    //{
        //while (isSpawning)
        //{ // dit zorgt ervoor dat de code elke 1 seconde wordt uitgevoerd
            //GameObject spawnedObject = Instantiate(gameObjectPrefab);
            //OnSpawnGameObject?.Invoke(spawnedObject);
            //yield return new WaitForSeconds(spawnInterval);
        //}
    //}
}
